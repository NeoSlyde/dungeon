package model.entities;

import model.misc.Position;
import model.misc.Size;
import view.View;

public class Zombie extends Monster{

    public Zombie(Position position, Size size, View view) {
        super(position, size, view);
    }
    
}
