package model.entities;

import model.misc.Position;
import model.misc.Size;
import view.View;

public class DefaultPlayer extends Player {

    public DefaultPlayer(Position position, Size size, View view) {
        super(position, size, view);
    }

    
}
